// URL constants
const BASE_URL = 'http://192.168.1.100:3000';

const SOCKET_CONN = BASE_URL;

const AUTH_ROUTES = BASE_URL + '/api/user';
const CHAT_META_ROUTES = BASE_URL + '/api/chat';

const REG_URL = AUTH_ROUTES + '/register';
const LOGIN_URL = AUTH_ROUTES + '/login';